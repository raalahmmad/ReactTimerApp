var React = require('react');

var Timer = React.createClass({
    render: function(){
        return(
            <div>
                I am the Timer
            </div>
        );
    }
});

module.exports = Timer;